var database = require('../database/config');

// Lista os USERs da farmaceutica
function xxxxxxxxxxxxxx(idMapa) {
  var instrucao = ``;

  return database.executar(instrucao);
}

module.exports = {
  xxxxxxxxxxxxxx
}